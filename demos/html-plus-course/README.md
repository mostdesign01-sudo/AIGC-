用浏览器直接打开本目录的 `index.html`（双击或拖进 Chrome / Edge / Safari，`file://` 即可，无需安装或构建）。门前大字 HTML+；点「进入课堂」淡入并轻微前推到开场。课堂里标题和正文可点改（点字可改），刷新后仍在。主讲用 `←` `→` / 空格翻页，点左侧章节或底部页点跳转；`F` 全屏，`S` 讲稿。把 T7 街拍 `t7-3.jpg` 放进本目录即可换成得物大楼。
